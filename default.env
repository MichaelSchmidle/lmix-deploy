# Supabase Configuration (required)
SUPABASE_POSTGRES_PASSWORD=your-secure-password
SUPABASE_JWT_SECRET=your-jwt-secret
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-key

# Port Configuration
# Default ports are chosen to be memorable (numpad layout):
# 5649 = LMIX, 5642 = LMIA, 5647 = LMIS
LMIX_PORT=5649            # Web UI port (default: 5649)
SUPABASE_API_PORT=5642    # API/Auth port (default: 5642)
SUPABASE_STUDIO_PORT=5647 # Studio port (default: 5647)

# Domain Configuration
# Three options for accessing LMiX:
# 1. Direct ports:      localhost:{port}
# 2. Local domains:     lmix.localhost, lmix-api.localhost
# 3. Public domains:    lmix.ai, api.lmix.ai
LMIX_DOMAIN=lmix.ai               # Web UI domain (default: lmix.ai)
LMIX_API_DOMAIN=api.lmix.ai       # API domain (default: api.lmix.ai)
LMIX_STUDIO_DOMAIN=studio.lmix.ai # Studio domain

# LMiX Configuration (auto-configured)
NUXT_PUBLIC_SUPABASE_URL=http://localhost:${SUPABASE_API_PORT}
NUXT_PUBLIC_SUPABASE_KEY=${SUPABASE_ANON_KEY}
